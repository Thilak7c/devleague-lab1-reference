# Copy this file to .env.local and set the actual backend URL.
# Defaults to http://localhost:8080 (see lib/api.js) if not set — fine for
# local dev against the backend-pipeline server running on its default port.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
